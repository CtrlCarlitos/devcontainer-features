{
    "id": "node",
    "version": "1.3.2",
    "name": "Node.js",
    "description": "Installs Node.js via nvm if not already present. Skips installation if Node.js is detected.",
    "options": {
        "version": {
            "type": "string",
            "default": "22",
            "description": "Node.js version to install if not present (e.g., '22', '20', 'lts')"
        }
    },
    "containerEnv": {
        "NVM_DIR": "/usr/local/share/nvm",
        "PATH": "/usr/local/share/nvm/current/bin:${PATH}"
    }
}